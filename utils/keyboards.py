from aiogram.utils.callback_data import CallbackData

select_difficulty = CallbackData("select_difficulty", "difficulty")

select_type = CallbackData("select", "difficulty", "type")

select_task = CallbackData("task", "pk")
